mutation Login($password: String!, $usernameOrNumberOrEmail: String!) {
  login(
    password: $password
    usernameOrNumberOrEmail: $usernameOrNumberOrEmail
  ) {
    errors {
      ...RegularError
    }
    user {
      ...RegularUser
    }
  }
}
